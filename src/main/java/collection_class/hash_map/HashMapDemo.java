package collection_class.hash_map;

import java.util.HashMap;

public class HashMapDemo {
    // Map is a key value pair where values can be
    // duplicated but keys cannot be duplicated
    // null key is allowed only once but multiple null values are allowed

    HashMap sa = new HashMap();// Declaring Hashmap


}
